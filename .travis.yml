language: generic
sudo: required
services: 
  - docker

before_install:
  - docker build -t jonsom/docker-react -f Dockerfile.dev .

scripts:
  - docker run -e CI=true jonsom/docker-react yarn test -- --coverage
